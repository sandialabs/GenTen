% GT_FINALIZE Finalize parallel resources for Genten
%
%  GT_FINALIZE finalizes parallel resources once Genten is finished.
%  This should only be called once in Matlab session.  It will be
%  called automatically when Matlab exits, so the user doesn't need to
%  call this directly.
%
%  See also GT_INTIALIZE
